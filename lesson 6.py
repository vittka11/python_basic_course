Ось приклад різниці між байтами та рядками.
# Створюємо рядок з українськими символами
s = "Привіт, світ!"

# Виводимо тип та довжину рядка
print(type(s)) # виведе: <class 'str'>
print(len(s)) # виведе: 11

# Кодуємо рядок в байти за допомогою кодування UTF-8
b = s.encode("utf-8")

# Виводимо тип та довжину байтів
print(type(b)) # виведе: <class 'bytes'>
print(len(b)) # виведе: 19
print(b) # виведе: b'\xd0\x9f\xd1\x80\xd0\xb8\xd0\xb2\xd1\x96\xd1\x82, \xd1\x81\xd0\xb2\xd1\x96\xd1\x82!'

# Декодуємо байти назад в рядок за допомогою кодування UTF-8
s = b.decode("utf-8")

# Виводимо тип та довжину рядка
print(type(s)) # виведе: <class 'str'>
print(len(s)) # виведе: 11
print(s) # виведе: Привіт, світ!

# Якщо вказати неправильне кодування, то отримаємо кракозябри
print(b.decode('windows-1251'))  # виведе: РџСЂРёРІС–С‚, СЃРІС–С‚!
Зауважте, що довжина рядка та байтів не однакова, оскільки деякі символи Unicode займають більше одного байта.

Байти
Байти в Пайтон оголошуються за допомогою префікса b перед рядком літералів, наприклад, b'hello'.

Байти можуть містити лише значення в діапазоні від 0 до 255,
які відповідають одному байту. Байти можна конвертувати в рядки за допомогою методу decode(),
який приймає кодування, наприклад, 'utf-8'.
Байти зазвичай використовуються для роботи з двійковими файлами, мережами, криптографією тощо.

На відміну від байтів, рядки можуть містити будь-які символи Unicode, які можуть займати від 1 до 4 байтів.

Різниця між байтами та рядками в Пайтон полягає в тому, що байти - це послідовності чисел,
які представляють двійкові дані, а рядки - це послідовності символів, які представляють текстову інформацію.

Метод encode() в Пайтон потрібен для того, щоб перетворити рядок в байти,
тобто послідовність чисел, які можна зберігати або передавати в інші системи.
Метод encode() приймає два параметри: кодування та спосіб обробки помилок.
Кодування - це правило, за яким кожен символ рядка відповідає певному числу.
Наприклад, кодування UTF-8 використовує від 1 до 4 байтів для представлення будь-якого символу Unicode.
Спосіб обробки помилок - це дія, яку потрібно виконати, якщо символ не може бути закодований за даним кодуванням.
Наприклад, спосіб 'ignore' просто пропускає такий символ, а спосіб 'replace' замінює його на спеціальний символ '?'.


